<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= title %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <style>
    body { 
      font-family: -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
      margin: 0;
      padding: 40px 20px;
      background: #f8fafc;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background: white;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }
    h1 {
      color: #1f2937;
      margin-bottom: 16px;
      font-size: 2rem;
    }
    p {
      color: #6b7280;
      margin-bottom: 32px;
      line-height: 1.6;
    }
    .nav-buttons {
      display: grid;
      gap: 16px;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    }
    .btn {
      padding: 16px 24px;
      border: 0;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 600;
      text-decoration: none;
      display: block;
      text-align: center;
      font-size: 16px;
      transition: all 0.2s;
    }
    .btn-primary {
      background: #2563eb;
      color: white;
    }
    .btn-primary:hover {
      background: #1d4ed8;
      transform: translateY(-1px);
    }
    .btn-secondary {
      background: #059669;
      color: white;
    }
    .btn-secondary:hover {
      background: #047857;
      transform: translateY(-1px);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Welcome to <%= title %></h1>
    <p>Manage your product inventory with ease. Add new products, view your existing catalog, and edit or delete products as needed.</p>
    
    <div class="nav-buttons">
      <a href="/add-product" class="btn btn-primary">Add New Product</a>
      <a href="/display-products" class="btn btn-secondary">View All Products</a>
    </div>
  </div>
</body>
</html>
